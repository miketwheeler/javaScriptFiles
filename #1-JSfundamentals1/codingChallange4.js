/*
const bill = 275;
let tip = 0;

console.log(
  `The bill was ${bill}, the tip was ${
    bill * (tip = bill >= 50 && bill <= 300 ? 0.15 : 0.2)
  }, and the total value ${bill + bill * tip}.`
);

const bill2 = 40;
let tip2 = 0;

console.log(
  `The bill was ${bill2}, the tip was ${
    bill2 * (tip2 = bill2 >= 50 && bill2 <= 300 ? 0.15 : 0.2)
  }, and the total value ${bill2 + bill2 * tip2}.`
);

const bill3 = 430;
let tip3 = 0;

console.log(
  `The bill was ${bill3}, the tip was ${
    bill3 * (tip3 = bill3 >= 50 && bill3 <= 300 ? 0.15 : 0.2)
  }, and the total value ${bill3 + bill3 * tip3}.`
);
*/
