/*
const markMass = 78;
const johnMass = 92;
const markHeight = 1.69;
const johnHeight = 1.95;

const markMass2 = 95;
const johnMass2 = 85;
const markHeight2 = 1.88;
const johnHeight2 = 1.76;

let markHigherBMI;

//1st variation of the Formula
const bmi1Mark = markMass / markHeight ** 2;
const bmi1John = johnMass / johnHeight ** 2;
const bmi2Mark = markMass2 / markHeight2 ** 2;
const bmi2John = johnMass2 / johnHeight2 ** 2;

markHigherBMI = bmi1Mark > bmi1John;
console.log(
  "Does Mark have a higher BMI? Mark Mass & Height:",
  markMass,
  markHeight,
  "John Mass & Height:",
  johnMass,
  johnHeight,
  markHigherBMI
);
markHigherBMI = bmi2Mark > bmi2John;
console.log(
  "Does Mark have a higher BMI? Mark Mass & Height:",
  markMass2,
  markHeight2,
  "John Mass & Height:",
  johnMass2,
  johnHeight2,
  markHigherBMI
);

// 2nd variation of the Formula, Same Data
const bmi1MarkAlt = markMass / (markHeight * markHeight);
const bmi1JohnAlt = johnMass / (johnHeight * johnHeight);
const bmi2MarkAlt = markMass2 / (markHeight2 * markHeight2);
const bmi2JohnAlt = johnMass2 / (johnHeight2 * johnHeight2);

markHigherBMI = bmi1MarkAlt > bmi1JohnAlt;
console.log(
  "Does Mark have a higher BMI? Mark Mass & Height:",
  markMass,
  markHeight,
  "John Mass & Height:",
  johnMass,
  johnHeight,
  markHigherBMI
);
markHigherBMI = bmi2MarkAlt > bmi2JohnAlt;
console.log(
  "Does Mark have a higher BMI? Mark Mass & Height:",
  markMass2,
  markHeight2,
  "John Mass & Height:",
  johnMass2,
  johnHeight2,
  markHigherBMI
);

if (markHigherBMI) {
  console.log(`Mark's BMI ${bmi2Mark} is higher than John's ${bmi2John}!`);
} else {
  console.log(`John's BMI ${bmi2John} is higher than Mark's ${bmi2Mark}!`);
}
*/
